async function loadContent(){
  loadSuper();
 /*  const response = await fetch('/api/auth');

    const authority = (await response.json()).authority;

    switch(authority){
    case 'SUPER' :loadSuper();
        break;
    case 'ADMIN' :document.getElementById("main").innerHTML="no such function yet";
        break;
    default :document.getElementById("main").innerHTML="no such function yet";

     }
 */

}



